# Core infrastructure — set once during deployment (not editable in UI)
DATABASE_URL="postgresql://aml_app:dev@localhost:5432/aml?schema=sechub"
REDIS_URL="redis://localhost:6379"
PORT=3002
AUTH_SECRET="change-me-to-a-random-32-char-string"
AUTH_URL="http://localhost:3001"
SETTINGS_ENCRYPTION_KEY="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

# API keys (DeepSeek, NVD) are configured in the app:
# Settings → API Keys & Integrations
